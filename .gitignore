node_modules
dist
dist-ssr
*.local
.DS_Store
.vercel
.env*

# Rust program workspace — the compiled artifacts are ~226 MB and rebuildable,
# only the crate source is worth tracking.
program/target

# deploy-program.mjs writes this on first run: it holds the program's secret
# key, i.e. the upgrade authority for the deployed program. Never commit it.
program/program-keypair.json
