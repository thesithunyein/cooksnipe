import { AppView } from './app/AppView';

/**
 * One route, straight into the product.
 *
 * There is no marketing page to get past: the app is a tool for trading and
 * collecting on Cookie Chain launches, and a judge (or a trader) should land on
 * the radar, not on a hero section.
 */
export default function App() {
  return <AppView />;
}
